version: '3.8'
services:
  bank_simulator:
    build: 
    ports:
      
    volumes:
      
    command: 

  sonarqube:
    image: sonarqube:latest
    ports:
      - "9000:9000"
    environment:
      - SONAR_ES_BOOTSTRAP_CHECKS_DISABLE=true
